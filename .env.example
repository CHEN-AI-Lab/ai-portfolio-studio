# ─── 七牛云 Kodo（生产环境必须配置）─────────────────────────────────
# 1. 注册七牛云 → 实名认证 → 创建 Bucket（存储区域选最近的）
# 2. 个人中心 → 密钥管理 → 拿到 AccessKey / SecretKey
# 3. Bucket 设置 → 绑定域名（或使用默认测试域名，格式：<bucket>.s3-<region>.qiniucs.com）
# 4. Bucket 设置 → 权限管理 → 设为「公开」（否则上传的文件不可访问）
QINIU_ACCESS_KEY=your_qiniu_access_key
QINIU_SECRET_KEY=your_qiniu_secret_key
QINIU_BUCKET=your-bucket-name
# CDN 域名或默认测试域名，如 "cdn.example.com" 或 "my-bucket.s3-cn-east-1.qiniucs.com"
QINIU_DOMAIN=your-bucket.s3-cn-east-1.qiniucs.com

# ─── 网站地址（用于 SEO）─────────────────────────────────────────────
SITE_URL=http://localhost:3000