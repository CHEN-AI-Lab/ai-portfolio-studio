/**
 * Localized text lookup — delegates to shared/i18n.
 *
 * Usage:
 *   import { t } from '../i18n'
 *   t(locale, 'nav.home')          → '首页'
 *   t(locale, 'work.filterByCategory') → 'Filter by category'
 */
export { t } from '@shared/i18n'